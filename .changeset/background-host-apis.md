---
"nativite": minor
---

Implement the first stable background task host context contract with result semantics, task-scoped storage, logging hooks, cancellation placeholders, and versioned native persisted task-state models that use the public `BackgroundTaskStatus` keys.
