---
"nativite": minor
---

Add the `nativite/test` entrypoint with JavaScript stub-host helpers for app tests, chrome snapshot inspection, native event emission, bridge call mocking, and a separate coordinator-backed native harness command surface.
