---
"nativite": minor
---

Add the `nativite/background` public API for defining JavaScript background tasks, registering task entrypoints in Nativite config, and creating metadata manifests without serializing task runners.
