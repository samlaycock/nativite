---
"nativite": patch
---

Clarify that OTA bundle staging and application currently apply to iOS and macOS only, while Android OTA checks return `{ available: false }` until Android parity is implemented.
