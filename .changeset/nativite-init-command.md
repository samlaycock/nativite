---
"nativite": minor
---

Add `nativite init` to generate a minimal Nativite config for existing Vite projects and safely add the Nativite Vite plugin when the Vite config can be edited unambiguously.
