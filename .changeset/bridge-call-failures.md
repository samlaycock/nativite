---
"nativite": patch
---

Add production-safe `bridge.call()` failure handling with timeout support, `AbortSignal` cancellation, structured `NativiteBridgeError` codes, and strict mode rejection when no native bridge is available.
