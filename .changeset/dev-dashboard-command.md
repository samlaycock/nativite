---
"nativite": minor
---

Add an optional `nativite dev` status dashboard that checks the Vite dev server URL, reports configured native platform project paths, and points developers back to the existing Vite and native IDE workflows.
