---
"nativite": minor
---

Add generated native test harness support for iOS, macOS, and Android debug/test builds. Harness builds can load a test URL, register with a local coordinator using a per-run session token, and report native and WebView readiness without changing release bundle behavior.
