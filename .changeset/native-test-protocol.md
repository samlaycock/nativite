---
"nativite": patch
---

Document the debug-only native test protocol for coordinator-backed `nativite/test` harness commands, including lifecycle, message envelopes, capabilities, transport, versioning, timeout, cancellation, and security requirements.
