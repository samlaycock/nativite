---
"nativite": minor
---

Add the local native test coordinator used by `nativite test`, including per-run session tokens, harness registration validation, command routing, artifact/log handling, and launch fallback guidance.
