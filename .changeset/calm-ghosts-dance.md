---
"nativite": patch
---

Rename legacy `nk`-prefixed CSS custom properties to `--nv-*` across native templates, runtime helpers, tests, and documentation.

This removes a legacy package-name prefix so generated variables are now aligned with the current naming convention.
