name: 🧪 Native Test Suite

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/native-tests.yml"
      - "bun.lock"
      - "package.json"
      - "scripts/test-native-android.ts"
      - "scripts/test-native-ios.ts"
      - "scripts/test-generated-native-apps.ts"
      - "src/native/android/runtime/**"
      - "src/native/android/**"
      - "src/native/ios/runtime/**"
      - "src/native/ios/**"
      - "src/plugins/**"
  workflow_dispatch:

jobs:
  ios:
    name: iOS runtime tests
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run iOS native tests
        run: bun run test:native:ios

  android:
    name: Android runtime tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run Android native tests
        run: bun run test:native:android

  generated-ios:
    name: Generated iOS app smoke
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Compile and package generated iOS app
        run: bun run test:generated:native:ios

  generated-macos:
    name: Generated macOS app smoke
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Compile and package generated macOS app
        run: bun run test:generated:native:macos

  generated-android:
    name: Generated Android app smoke
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Compile and package generated Android app
        run: bun run test:generated:native:android
